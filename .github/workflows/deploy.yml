name: Build & Deploy Go Backend

on:
  push:
    branches: [master]

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Download deps
        run: go mod download

      - name: Run tests
        run: go test ./...

      - name: Build Linux binary
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o app ./cmd

      - name: Upload binary to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: app
          target: /var/www/be-go-flush
          overwrite: true

      - name: Restart service
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/be-go-flush
            chmod +x app
            sudo systemctl restart be-go-flush

      - name: Discord notify
        if: always()
        run: |
          curl -H "Content-Type: application/json" \
          -d '{
            "username": "Deploy Bot",
            "embeds": [{
              "title": "🚀 Go Backend Deploy",
              "description": "**Branch:** ${{ github.ref_name }}\n**Commit:** ${{ github.sha }}\n**Status:** ${{ job.status }}\n**Actor:** ${{ github.actor }}",
              "color": 3066993
            }]
          }' \
          https://ptb.discord.com/api/webhooks/1469429197197017089/a8kJUQnFkTaKGAW8DZbeQSkAlPvCZpbH4MKg5AZEwYRF2zOI3T-9P2tEKVjalhJ48mqR
